package com.jaidensiu.quickmaths.domain

enum class Operation(val symbol: String) {
    ADDITION(symbol = "+"),
    SUBTRACTION(symbol = "−"),
    MULTIPLICATION(symbol = "×"),
    DIVISION(symbol = "÷"),
}
